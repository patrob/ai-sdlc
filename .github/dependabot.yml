version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    ignore:
      # Ignore major version updates - only allow minor and patch
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
